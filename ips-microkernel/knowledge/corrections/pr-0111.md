# PR #111 implementation corrections

<!-- ips-data: implementation-correction-occurrences -->

### Occurrence 1

- **PR:** PR #111
- **Mistake:** The environment root referenced an operator role whose identity policy and fixed permissions boundary did not effectively allow all AWS provider calls required to apply the declared resources.
- **Correction:** Replaced the unpublished Terraform IAM change with separately named Console-owned OperatorPermissions and OperatorBoundary definitions, accepted exact Console-owned role ARNs, and added a provider-version-pinned action matrix that verifies every planned operation against both policy layers.

### Occurrence 2

- **PR:** PR #111
- **Mistake:** The first live apply exposed provider-dependent tag authorization calls, an EC2 prefix-list read, and an API Gateway service-linked-role prerequisite that the initial static action matrix did not model.
- **Correction:** Added the exact tag and prefix-list actions to the Console-owned OperatorPermissions contract, recorded all four service-linked roles as owner-created prerequisites, and extended the two-layer action-matrix proof before retrying the saved environment configuration.

### Occurrence 3

- **PR:** PR #111
- **Mistake:** The second live apply proved that recovery from a partially created Security Group rule set requires `ec2:ModifySecurityGroupRules`, although a clean create-only plan does not exercise that action.
- **Correction:** Added the exact rule-modification action to OperatorPermissions and to both ingress and egress lifecycle rows in the two-layer action matrix before the final construction attempt.

### Occurrence 4

- **PR:** PR #111
- **Mistake:** The post-create real plan reached three provider refresh helpers that were absent from the pre-create matrix: Cognito client listing, MQ shared-resource description, and Secrets Manager resource-policy reading.
- **Correction:** Added only those three read actions to OperatorPermissions and their owning resource rows, and required a successful post-create plan before consuming the final construction attempt.

### Occurrence 5

- **PR:** PR #111
- **Mistake:** The MQ resource used accepted but non-canonical enum casing, so provider 6.58.0 read back `RabbitMQ`, `simple`, and `ebs` and produced a perpetual broker replacement after the first real refresh.
- **Correction:** Switched the MQ configuration and plan assertions to the provider's canonical read-back casing while preserving the RabbitMQ 4.2, EBS, and simple-authentication contract.

### Occurrence 6

- **PR:** PR #111
- **Mistake:** The third and final construction attempt exposed two delegated create dependencies absent from the matrix: API Gateway tags authorize through `apigateway:TagResource`, and Cloud Map private namespace creation invokes Route 53 hosted-zone creation under the caller.
- **Correction:** Added exact API Gateway tag rows, modeled Cloud Map's documented Route 53 create/read dependencies as service-delegated actions, and stopped further construction after the governed `3/3` attempt limit.

### Occurrence 7

- **PR:** PR #111
- **Mistake:** The first owner-authorized destroy reached provider cleanup paths that inspect and detach managed network interfaces, but the separate destroy policy omitted `ec2:DescribeNetworkInterfaces` and `ec2:DetachNetworkInterface`.
- **Correction:** Added both actions only to the Console-owned DestroyPolicy, retained the independent OperatorPermissions grant and OperatorBoundary ceiling, and extended static proof to require effective destroy cleanup authority without granting network-interface detachment to the operator role.

### Occurrence 8

- **PR:** PR #111
- **Mistake:** The destroy-authority verifier correction was pushed before the canonical Ruff formatting check, so the complete static verification stopped at API formatting despite the focused AWS proof passing.
- **Correction:** Applied canonical Ruff formatting to the verifier, retained the effective-authority assertions unchanged, and restarted publication Gate A from the corrected committed head.

### Occurrence 9

- **PR:** PR #111
- **Mistake:** The human-readable implementation record was corrected to the consumed `3/3` construction limit, but both AWS-free evidence generators still emitted the obsolete pre-evaluation `0/3` value.
- **Correction:** Updated bootstrap and environment machine evidence to report the same governed `3/3` historical attempt count, preserving zero AWS calls, writes, and resources as properties of each static verifier rather than conflating them with live history.

### Occurrence 10

- **PR:** PR #111
- **Mistake:** The first Console-owned DestroyPolicy grouped generated-ID and dependent cleanup actions under `Resource: "*"` without immutable ownership or AWS service-delegation conditions, so unrelated account resources could satisfy the destroy identity grant.
- **Correction:** Removed the undocumented Route 53 delete grant; split remaining destroy authority into complete ownership-tag, exact environment-name ARN, and AWS-supplied Amazon MQ `aws:CalledVia` statements; isolated AWS's resource-global task-definition deregistration action; and added paired cross-environment, cross-repository, unmanaged, persistent, direct-call, wrong-service, quota, and adversarial boundary proofs.

### Occurrence 11

- **PR:** PR #111
- **Mistake:** The live API Gateway `TagResource` operation was copied into the IAM policy as `apigateway:TagResource`, but API Gateway Management V2 authorizes that operation with its HTTP-method IAM actions and AWS Access Analyzer correctly rejected the invented action.
- **Correction:** Mapped the provider's tag calls to `apigateway:POST` on the API, Stage, and VPC Link resource forms, removed the invalid action, and made the AWS-free verifier reject every API Gateway IAM action outside the exact reviewed method set.

### Occurrence 12

- **PR:** PR #111
- **Mistake:** The first Security Group proof compared only protocol/port/CIDR multisets and a separately authored edge count, so a same-port source/destination cross-wire could retain every passing aggregate.
- **Correction:** Bound every rule's Terraform configuration references to the exact source and referenced Security Group identities, fixed the complete expanded `for_each` address set, and retained the independent protocol/port/CIDR inventory checks.

### Occurrence 13

- **PR:** PR #111
- **Mistake:** The corrected provider action inventory proved only positive operator decisions while one unconditional `Resource: "*"` identity statement still made unrelated Secrets Manager reads/writes and cross-environment Security Group mutations effective through the coarse boundary.
- **Correction:** Removed that statement; retained `OperatorPermissions` and `OperatorBoundary` as separate static objects; split environment creation and resource authority into quota-safe customer-managed policies; bound every write to exact request tags, the existing ownership tuple, or deterministic environment ARN patterns; and added identity plus effective inverse proof for every provider action mode.

### Occurrence 14

- **PR:** PR #111
- **Mistake:** The architecture index and Step 4 delivery narrative still said the environment had never been applied after the owner-authorized exploratory evaluation had truthfully consumed `3/3` attempts elsewhere in the same change.
- **Correction:** Reconciled both durable records to the unsuccessful bounded evaluation, zero final state, zero application residue, Step 7 non-completion, and prohibition on a fourth construction apply.

### Occurrence 15

- **PR:** PR #111
- **Mistake:** Cloud Map `TagResource` was treated as a creation-like request-tag action even though AWS authorizes it only at `Resource: "*"`; the operator could therefore overwrite the ownership tuple on an unrelated existing namespace or service and make later tag-gated destroy authority accept it.
- **Correction:** Removed standalone Cloud Map tagging from both the operator identity and boundary, retained ownership tags passed directly by provider 6.58.0 in `CreatePrivateDnsNamespace` and `CreateService`, documented those Cloud Map tags as immutable, and added identity, boundary, and effective foreign-target retag denials.

### Occurrence 16

- **PR:** PR #111
- **Mistake:** The correction inferred the complete Cloud Map authorization set from the provider create payload, so the Console policies removed the dependent `TagResource` action that AWS maps to both create operations while the bootstrap implementation retained it; Cloud Map creation became effectively denied and the two static authority paths plus their documentation disagreed.
- **Correction:** Restored `TagResource` in the separate static identity and boundary, aligned Console and bootstrap matrices, derived the required pair from AWS's operation-to-IAM mapping, and recorded the owner's dedicated-account exception truthfully with exact-tag inverse checks, explicit foreign-target allows, and before/after Cloud Map inventory gates.

### Occurrence 17

- **PR:** PR #111
- **Mistake:** Static evidence still exposed bare `awsApiCalls`, `awsWrites`, and `awsResourcesCreated` zero fields plus a `0/0/0` summary, which could be read as Issue-wide history even though real AWS policy, role, boundary, assumption, construction, and cleanup operations had already occurred.
- **Correction:** Versioned both evidence schemas, renamed every zero to `staticVerifier...`, added `liveAwsHistoryIncluded=false`, preserved the bounded construction count as explicitly historical, and changed the console and delivery wording so verifier-local zeros cannot erase real AWS activity.

### Occurrence 18

- **PR:** PR #111
- **Mistake:** The Console-owned operator trust required MFA even though the owner selected access-key authentication for the exact Noel IAM principal and did not require an MFA-gated role session.
- **Correction:** Removed the MFA condition from the static operator trust in both the repository and live AWS role, retained the exact principal plus same-account restriction, kept deployment authority on the assumed role rather than the IAM user, and added an exact trust-document assertion to the AWS-free verifier.

### Occurrence 19

- **PR:** PR #111
- **Mistake:** The first no-MFA correction aligned the Console trust but left the bootstrap input restricted to role ARNs, so the same human operator contract could not represent the owner-selected exact IAM-user principal through both maintained static paths.
- **Correction:** Allowed an explicit same-account IAM user or role ARN in bootstrap, changed the synthetic contract to exercise a user principal, aligned the trust statement identity, and asserted the exact principal, same-account condition, and absence of an MFA condition in the Terraform mock plan.

### Occurrence 20

- **PR:** PR #111
- **Mistake:** Successful role assumption was initially treated as sufficient evidence for the source IAM user, but the live administrator identity policy allowed `sts:AssumeRole` on every role and therefore did not provide the intended exact source boundary.
- **Correction:** Reused the existing credential-only `ReactorFrontNoel` IAM user instead of creating another user, attached a managed exact operator-role policy, moved the operator trust to that existing user ARN, retained its current access key and no-Console-login state, and added positive plus inverse static and live simulations.

### Occurrence 21

- **PR:** PR #111
- **Mistake:** The first source-user correction over-constrained the permanent identity to only the operator role and would have removed the owner's explicitly desired billing-price and observer sessions.
- **Correction:** Kept the three approved exact role paths, converted the two legacy billing/observer inline policies into separate Console-owned customer-managed policies, left the source user with no direct AWS resource authority, and proved that operator, billing-read, and observer are allowed while destroy, unrelated roles, and direct EC2/S3/RDS deployment calls are denied.
