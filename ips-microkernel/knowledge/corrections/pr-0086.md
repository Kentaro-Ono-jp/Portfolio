# PR #86 implementation correction occurrences

<!-- ips-data: implementation-correction-occurrences -->

### Occurrence 1

- **PR:** PR #86
- **Mistake:** The promoted-model integer artifact schema version lacked direct boolean-discriminator rejection proof at the production parser before evidence loading.
- **Correction:** Added canonical `true` and `false` manifest mutations that require `PROMOTION_SCHEMA_VIOLATION` with zero dataset-evidence loads.
