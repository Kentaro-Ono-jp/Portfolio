# PR #64 implementation-correction occurrences

<!-- ips-data: implementation-correction-occurrences -->

### Occurrence 1

- **PR:** PR #64
- **Mistake:** The Stage A, Stage B, and CI Playbook validators filtered for
  allowed labels, so unknown field headings and blank required values could
  pass documentation verification.
- **Correction:** Parse every record field heading, require the exact ordered
  schema with non-empty values, and add focused negative mutations for unknown
  fields and every blank required field.
