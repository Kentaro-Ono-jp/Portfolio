{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "ExactEnvironmentDestroyProject",
      "Effect": "Allow",
      "Principal": {"Service": "codebuild.amazonaws.com"},
      "Action": "sts:AssumeRole",
      "Condition": {
        "StringEquals": {"aws:SourceAccount": "${AWS_ACCOUNT_ID}"},
        "ArnLike": {
          "aws:SourceArn": "arn:${AWS_PARTITION}:codebuild:${AWS_REGION}:${AWS_ACCOUNT_ID}:project/${NAME_PREFIX}-${ENVIRONMENT}-destroy"
        }
      }
    }
  ]
}
