{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "ListApplicationObjects",
      "Effect": "Allow",
      "Action": "s3:ListBucket",
      "Resource": "arn:${AWS_PARTITION}:s3:::${NAME_PREFIX}-${ENVIRONMENT}-documents"
    },
    {
      "Sid": "ReadAndWriteApplicationObjects",
      "Effect": "Allow",
      "Action": ["s3:GetObject", "s3:PutObject"],
      "Resource": "arn:${AWS_PARTITION}:s3:::${NAME_PREFIX}-${ENVIRONMENT}-documents/*"
    }
  ]
}
